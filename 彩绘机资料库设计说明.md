# 彩绘机资料库设计说明

## 本次采集结果

采集日期：2026-09-10。来源：[民航休闲小站机队目录](http://www.xmyzl.com/?mod=jidui)。

- 遍历网站标出的 25 页目录，去重后读取 197 个机队详情页，包含航司、分公司和通航公司。
- 36 个详情页包含明确的当前彩绘／特别涂装栏目，其中 1 页明确标注 0 架。
- 从其余 35 个栏目提取 343 个不同注册号，已写入 `data/liveries.sqlite`。
- 161 个详情页未发现这一栏目，属于“该来源没有明确列出”，不能判定这些航司没有彩绘机。网站部分末尾目录页为空，仍已检查全部分页。
- 包含联盟、纪念、联名、主题等源站明确列为特别涂装的飞机，不仅是整机大面积彩绘；不收录普通机队注册号、历史新闻、退役条目或图片推断。

网站正文与栏目标题数量存在以下差异，已核查正文且按实际列出的注册号保存，不自行补造或删减：

| 航司 | 标题数量 | 正文注册号数量 |
| --- | ---: | ---: |
| 中国东方航空 | 34 + 15 = 49 | 51 |
| 河北航空 | 6 | 7 |
| 西部航空 | 8 | 7 |
| 湖南航空 | 6 | 7 |

上述差异保存在 JSON 快照各页面的 `notices` 字段；真正无法识别的行保存在 `warnings` 并阻止导入。

## 采集与解析

`scripts/collect_xmyzl.py` 使用 Python 标准库请求公开 HTML，不登录、不调用付费接口、不下载图片、不绕过访问控制。

1. 检查 robots.txt；本次返回 404。后续若禁止目标路径则停止相应采集。
2. 遍历目录分页，按机队详情 ID 去重。请求串行且至少间隔 1.1 秒，并尊重更长的 Crawl-delay。
3. 只识别明确的当前彩绘栏目，遇到近期机队消息、历史或退役等边界即停止。
4. 支持“名称：多个注册号”“注册号 名称”、字母混合注册号、港台五位数字注册号、全角连字符和单机括号备注。
5. 保留事实证据行及来源 URL，不将整页文章写入对外快照。原始 HTML 放在被 Git 忽略的 `.runtime/xmyzl/` 中，便于离线核查。

默认命令复用已下载页面，不表示资料刷新到了运行当天。每条记录的 `observedAt` 来自页面实际下载时间，而非离线重新解析时间。`--refresh` 才重新下载；`--cached` 禁止联网。

所有运行报告写到 `.runtime/xmyzl-last-report.json`。网络失败、返回非机队页面或栏目存在未知格式时，退出码非零且保留上一份有效种子快照。源站无栏目不能被解释为“全部停用”。

## 数据模型与导入规则

SQLite 文件位于项目根目录下的 `data/liveries.sqlite`，位置不依赖命令执行时的工作目录。

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `special_liveries` | `registration`, `name`, `airline`, `source_url`, `evidence`, `first_seen_at`, `last_seen_at`, `active` | 注册号匹配、来源追溯、旧记录停用 |
| `livery_sources` | `source_url`, `airline`, `observed_at`, `section_count` | 已识别栏目的来源与最近导入时间 |

`special_liveries` 的联合主键为注册号 + 涂装名称 + 来源 URL，并有注册号／active 联合索引。相同注册号可以保留多个来源，展示时合并名称。注册号统一大写，去掉空白，补齐 B 前缀之后的连字符。

导入过程使用单个事务：校验整个快照 → 停用完整栏目对应来源的旧记录 → upsert 新记录 → 提交。重复导入不会增加重复行；旧涂装不物理删除，保留首次和末次观测时间。页面缺失、解析异常、记录来源不符、无效注册号、时间回退或写入失败均拒绝导入或回滚。没有当前栏目时，不自动停用该来源既有数据，需要人工复核。

首次读取空库时自动导入仓库内的 JSON 快照。现有数据库不会仅因种子文件更新而被覆盖，需要明确运行导入命令。数据库、运行缓存均忽略 Git；JSON 快照随项目保留，使新环境可复现初始化。公开分发来源数据前需另行确认源站授权，不因页面公开便假定允许商业再分发。

运行时优先使用 Node.js 内置 SQLite。为兼容现有 Node.js 20.19，使用固定版本的 `better-sqlite3` 12.9.0 后备驱动；pnpm 配置已允许其安装脚本。新环境执行 `pnpm install` 时需要访问官方预编译包下载地址；推荐 Node.js 24，避免对原生兼容驱动的运行依赖。

## 航班查询接入

`specialFlightRules.mjs` 从数据库读取 active 记录，对计划／补全航班的注册号精确匹配，命中后加入 `special-livery` 分类，并返回涂装名称、来源列表、采集时间及可解释理由。前端详情展示这些字段。

机场范围仍严格限制上海浦东和上海虹桥；机库覆盖各航司，是为了识别它们飞抵上海的飞机，不扩大机场查询范围。

机场缓存保留原始合并航班。每次命中缓存时也重新读取彩绘库进行分类，因此数据导入后普通刷新即可生效，不用重新调用航班 API。数据库读写短事务及 5 秒忙等待适用于当前单实例 MVP；多实例部署需要另行设计共享存储和更新协调。

采集日期不是涂装生效日期。查询早于采集日期的历史航班时，不使用这份当前快照作为涂装证据；当前或后续航班只能列为资料命中候选，不能据此保证涂装仍有效。转籍、改装、退役、注册号重用及源站延迟都可能导致资料过期。

没有注册号的航班不猜测彩绘，现有候选补全额度也不因这次改动增加。因此免费航班源未提供机尾号时，仍可能漏报彩绘机；本库不会单独生成飞机当天到访的时间表。

## 维护与验证

```bash
pnpm liveries:collect --refresh
# 确认采集成功；检查报告中的 notices 后再导入
pnpm liveries:import
pnpm test:liveries
pnpm build
```

首次更新代码后重新启动前后端；以后仅更新资料无需重启。健康检查 `/api/health` 中的 `liveryDatabase` 显示注册号数量、航司数量及最近观测时间。

Python 测试覆盖不同排列格式、备注、实体字符、未知格式以及排除新闻／退役。Node 测试使用内存数据库验证去重、停用、事务回滚、时间回退保护及实际快照的正负匹配；不调用航班 API 或消耗额度。

例如 B-2006 对应国航“爱中国”，B-1151 对应中联航“千里江山-携程旅行”。这仅验证本地来源资料匹配，不是这些飞机当天飞往上海的声明。
