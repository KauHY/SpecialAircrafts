# 特别飞机查询系统 Web MVP 设计文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 航迹志 · 特别飞机查询系统 |
| 当前阶段 | 第一阶段 MVP |
| 交付形态 | 响应式 Web 应用 |
| 技术栈 | React 19、TypeScript、Vite、Express、Node.js |
| 数据状态 | 飞常准 Aviation MCP 第一顺位 + AeroDataBox 第二顺位 + ADSB.lol 补充 |

## 2. MVP 目标

第一阶段验证的核心问题不是“能否展示完整航班表”，而是用户能否用最少操作发现某个机场今天值得关注的飞机。

本阶段形成如下最小闭环：

1. 用户输入机场名称、IATA 或 ICAO 代码。
2. 系统返回该机场当天的特别飞机摘要。
3. 用户按特别涂装、特殊机型、稀有航司和临时包机筛选。
4. 用户查看单架飞机的航班、注册号、机型、到访频率和入选原因。
5. 页面明确标注数据来源、连接状态、更新时间和可信度。

暂不包含账户体系、推送订阅、地图实时轨迹、社区投稿、图片版权管理和付费会员功能。

## 3. 信息架构

```text
首页
├── 顶部导航
├── 机场搜索区
│   ├── 机场名称 / IATA / ICAO 输入
│   └── 快捷机场入口
├── 机场概览
│   ├── 机场身份信息
│   ├── 今日发现数量
│   ├── 数据更新时间
│   └── 天气、能见度与跑道提示
├── 今日特别飞机
│   ├── 类型筛选
│   ├── 特别飞机卡片
│   └── 空状态
├── 飞机详情侧栏
└── MVP 与数据免责声明
```

## 4. 页面与交互设计

### 4.1 视觉定位

整体采用“航行情报终端 + 航空杂志”的视觉方向：

- 深海军蓝体现专业、可靠和机场夜间运行感。
- 橙色作为重点操作、飞机位置与稀有度提示色。
- 米白色和浅灰色降低纯工具界面的冷硬感。
- 英文窄体字体用于机场代码、机型和数值信息，中文字体负责叙述内容。
- 卡片层级较克制，使用户优先阅读飞机本身而非装饰元素。

### 4.2 机场搜索

- 默认查询 PVG；没有真实数据源时展示配置提示，不展示虚构航班。
- 支持机场中文名、城市、IATA 和 ICAO 的模糊匹配。
- 本地机场目录覆盖 47 个常用机场，任意有效 IATA/ICAO 代码均可提交后端查询。
- 热门机场提供 PVG、PEK、CAN、HKG、NRT、SIN、LHR、LAX 快捷入口。
- 查询过程调用本项目后端，由后端处理供应商密钥和数据聚合。
- 无结果时保留当前查询结果，并在搜索框下展示可恢复的错误信息。

### 4.3 机场概览

机场概览回答用户到场前最关心的四件事：

- 今天发现了多少架特别飞机。
- 有多少架属于高推荐目标。
- 当前数据何时更新。
- 天气、风向、能见度和预计进场跑道情况。

正式版本中的跑道方向只作为概率提示，应同时展示来源与更新时间，不应承诺实际运行跑道。

### 4.4 特别飞机卡片

卡片的信息优先级为：

1. 特别类别及当前状态。
2. 航司、机型、注册号及航班号。
3. 始发地、目的地和预计到达时间。
4. 推荐值及被系统选中的原因。

整张卡片均可点击，打开右侧详情面板。相比跳转新页面，侧栏能保持用户对当天飞机列表的上下文。

### 4.5 分类筛选

MVP 使用单选筛选，避免多条件组合产生难以理解的空结果。分类定义如下：

| 分类 | 判断方向 |
| --- | --- |
| 特别涂装 | 注册号命中特别涂装资料库且当前仍有效 |
| 特殊机型 | 低频机型、经典机型、货机、公务机或机场非常态机型 |
| 稀有航司 | 航司在目标机场的历史到访频率明显低于常驻航司 |
| 临时包机 | 包机、调机、临时任务或非固定班期航班 |

### 4.6 响应式设计

- 大屏：双列飞机卡片，机场统计为四列。
- 平板：单列卡片，机场统计为两列。
- 手机：搜索按钮收缩为图标，筛选栏横向滚动，详情侧栏占满屏幕。
- 支持 `prefers-reduced-motion`，减少对动画敏感用户的不适。

## 5. 前端架构

```text
src/
├── components/
│   ├── AirportOverview.tsx   # 机场及天气概览
│   ├── FlightCard.tsx        # 特别飞机摘要卡片
│   ├── FlightDrawer.tsx      # 飞机详情侧栏
│   ├── Icons.tsx             # 项目内联 SVG 图标
│   └── SearchPanel.tsx       # 搜索与快捷机场
├── services/
│   └── aircraftService.ts    # 后端 API 客户端
├── App.tsx                   # 页面状态与组合
├── main.tsx                  # 应用入口
├── styles.css                # 设计系统与响应式样式
└── types.ts                  # 领域类型
```

### 5.1 状态设计

当前状态保持在 `App` 层，包含：

- 当前机场快照。
- 当前筛选类型。
- 当前选中的特别飞机。
- 查询加载状态。
- 查询错误信息。

第一阶段状态规模较小，无需引入全局状态库。进入账户、收藏、跨页面共享缓存阶段后，再评估 Zustand 或 Redux Toolkit。

### 5.2 数据访问层

组件调用 `aircraftService`，再由后端访问供应商。密钥不会下发给浏览器。后端将供应商字段统一为 `AirportSnapshot`，前端不依赖任何供应商原始格式。

当前已经实现以下接口：

```http
GET /api/health
GET /api/airports/search?q=PVG
GET /api/airports/PVG/special-flights?date=2026-08-20
```

机场当日接口建议响应：

```json
{
  "airport": {
    "iata": "PVG",
    "icao": "ZSPD",
    "name": "上海浦东国际机场",
    "timezone": "Asia/Shanghai"
  },
  "date": "2026-08-20",
  "lastUpdated": "2026-08-20T10:32:00+08:00",
  "weather": {},
  "flights": []
}
```

日期应按机场本地时区计算，而不是按用户设备时区计算。

## 6. 领域数据模型

### 6.1 Airport

- `iata`：三字机场代码。
- `icao`：四字机场代码。
- `name`、`city`、`country`：显示与搜索字段。
- `timezone`：决定“今天”的日期边界。

### 6.2 SpecialFlight

- 航班身份：航班号、呼号、航司、出发地和目的地。
- 飞机身份：ICAO 机型代码、机型全名和注册号。
- 运行信息：计划时间、预计时间、状态及航站区域。
- 特别属性：分类、推荐值、入选理由、特别涂装和近期到访次数。

注册号是连接航班动态、航空器档案与彩绘机资料库的关键字段，但实际数据可能在航班起飞后才出现，前端必须允许该字段暂缺。

## 7. “特别程度”评分初案

当前版本根据特殊机型、涂装注册号、航班服务类别和同日航司频率生成候选分数，并保留可解释的入选理由。历史到访数据尚未接入，因此不会伪造“近 30 日到访次数”。长期目标模型如下：

```text
总分 = 机型稀有度 × 30%
     + 航司稀有度 × 25%
     + 涂装特别度 × 25%
     + 到访临时性 × 15%
     + 社区关注度 × 5%
```

建议分级：

| 分值 | 级别 | 产品含义 |
| --- | --- | --- |
| 90–100 | 极高 | 强烈推荐，通常为罕见机型或限期彩绘 |
| 80–89 | 高 | 明显低频，值得关注 |
| 70–79 | 中 | 对特定兴趣用户有价值 |
| 60–69 | 观察 | 可显示，但不进入重点提醒 |
| 低于 60 | 普通 | 默认不列入特别飞机清单 |

评分必须按“机型 + 航司 + 目标机场 + 时间窗口”组合计算。仅凭机型绝对稀有度会造成偏差，例如某宽体机在枢纽机场很常见，但在支线机场可能非常罕见。

## 8. 免费优先的数据接入设计

建议后端建立统一数据归一层，不让前端直接依赖单个供应商字段。

```text
飞常准旧版 V3 / AeroDataBox ─> 顺位选择 / 归一去重 ─> 初次特别规则筛选 ─┐
飞常准 Aviation MCP ─────────────<──── 按航班号优先校验最多 4 个候选 ─┤
ADSB.lol 候选补充 ───────────────<──── 仅补充仍缺注册号/机型的候选 ───┤
彩绘机注册号资料库 ──────────────────────────────────────────┘
```

关键处理任务：

- 航班号、呼号和注册号的关联。
- 计划时间、预计时间和实际时间的状态合并。
- 代码共享航班去重。
- 机型代码统一，如运营方名称与 ICAO 代码映射。
- 机场本地日期转换。
- 彩绘有效期维护和图片版权记录。
- 数据源置信度、更新时间与冲突解决。

### 8.1 为什么选择该组合

| 方案 | 角色 | 选择原因 | 不承担的职责 |
| --- | --- | --- | --- |
| 飞常准 Aviation MCP | 第一顺位候选校验 | 中国及国际航班覆盖强；单 API Key；按航班号补充状态、时间和机型 | 路线工具不能只凭到达机场列出完整 FIDS |
| AeroDataBox RapidAPI FREE | 第二顺位后备 | 免费计划长期有效；机场 FIDS 能返回航班、计划时间、航司、机型和可能的注册号；覆盖全球机场 | 高频实时轨迹和无限查询 |
| ADSB.lol | 候选补充 | 无需密钥、开放数据、可按呼号查询当前可见飞机 | 完整未来航班表、保证覆盖率 |
| FlightAware / FR24 | 可选增强 | 已有适配器，可在取得正式授权和预算后提高部分地区质量 | MVP 运行的必需依赖 |

OpenSky 等开放 ADS-B 网络适合研究与位置数据，但不提供足够完整的商业航班时刻表；Aviationstack 等免费档额度较小或许可限制不适合作为本产品默认主源。由于产品只需回答“今天大概何时会出现”，低频计划快照比持续实时轮询更匹配需求。

### 8.2 调用与额度模型

AeroDataBox 机场 FIDS 的单次时间范围最多 12 小时，属于 Tier 2。查询机场本地完整一天时，后端顺序请求两个窗口：

```text
00:00 ───────── 12:00 ───────── 23:59
       2 units           2 units

一个机场日快照 ≈ 4 API units
```

免费计划公开额度为 600 API units/月。MVP 默认设置 500 units 的项目侧预算，理论上可生成约 125 个“机场 + 日期”的首次快照，并留出约 100 units 安全余量。预算是本地防误用机制，不代替 RapidAPI 控制台的真实额度。

### 8.3 缓存与容错

- 新快照在进程内缓存 12 小时；相同机场和日期的重复访问不请求上游。
- 失败时可回退到 24 小时容错期内的上一份内存快照，并在页面明确标为缓存。
- `.runtime/api-usage.json` 按 UTC 自然月记录 AeroDataBox 已预留 units；每次上游调用前先记账，失败请求也视为已消耗。
- 两个 FIDS 请求串行且至少间隔 1.5 秒，为免费档 1 req/s 的边界计时留出余量。
- 相同机场和日期的并发请求共用一个进行中的 Promise，避免 React Strict Mode 等场景重复打到上游。
- 瞬时网络错误、HTTP 429 或上游 5xx 最多自动重试一次；鉴权与参数错误不会盲目重试。
- `refresh=1` 只跳过快照缓存，不跳过预算控制。
- 多实例部署必须将快照和额度账本迁移至 Redis/数据库，并使用原子计数。

### 8.4 ADSB.lol 补充边界

系统先用计划数据执行一次特别规则筛选，只对已成为候选且仍缺少注册号或机型的航班按呼号查询 ADSB.lol。每个快照默认最多 4 次，结果缓存 15 分钟。这样可避免对数百架普通航班逐一查询开放服务，也不会把实时 ADS-B 是否可见误当成当天计划是否存在。

## 9. 已实现的后端聚合层

```text
server/
├── data/
│   ├── airports.mjs            # 机场目录与代码解析
│   └── specialRegistry.mjs     # 特殊机型和彩绘注册号
├── providers/
│   ├── aerodatabox.mjs         # 免费计划主源、双 12 小时窗口与限速
│   ├── adsblol.mjs             # 候选飞机开放 ADS-B 补充
│   ├── variflight.mjs          # 飞常准 MCP Key / 旧版签名与统一映射
│   ├── flightaware.mjs         # AeroAPI v4 适配器
│   ├── flightradar24.mjs       # FR24 实时入港适配器
│   └── http.mjs                # 超时和安全错误处理
├── services/
│   ├── aggregator.mjs          # 并行请求、去重和字段合并
│   ├── quotaLedger.mjs         # 单实例月度 API units 安全账本
│   └── specialFlightRules.mjs  # 特别程度规则
└── index.mjs                   # Express API 与生产静态服务
```

聚合器默认使用 `priority` 策略：真正的飞常准旧版 V3 凭据可作为机场计划第一顺位，否则由 AeroDataBox 获取完整 FIDS。初筛后，飞常准 Aviation MCP 按航班号优先校验最多 4 个特别候选，并让其有效字段覆盖计划来源；再由 ADSB.lol 补缺。设置 `PROVIDER_STRATEGY=aggregate` 后才会并行聚合全部启用计划来源；FlightAware/FR24 仍需额外设置 `ENABLE_COMMERCIAL_PROVIDERS=true`。

| 字段 | 首选来源 | 补充来源 |
| --- | --- | --- |
| 计划时间 | 飞常准 | AeroDataBox / FlightAware |
| 预计、实际时间 | 飞常准 | FlightAware / AeroDataBox / FR24 ETA |
| 注册号、ICAO 机型 | 飞常准 / ADSB.lol | AeroDataBox / FR24 / FlightAware |
| 实时位置 | FR24 | FlightAware last position |
| 航站楼与状态 | 飞常准 / FlightAware | FR24 跟踪状态 |
| 特别涂装 | 自建注册号资料库 | 授权数据源或人工审核 |

### 9.1 后端继续演进建议

下一步可采用如下结构：

- API：Node.js + Fastify/NestJS，或团队熟悉的同类框架。
- 主数据库：PostgreSQL，保存机场、航空器、航班、涂装和评分结果。
- 缓存：Redis，缓存机场当日列表和频繁查询。
- 定时任务：按机场热度更新航班动态并重算推荐值。
- 可观测性：记录供应商响应时间、匹配失败率、数据新鲜度和接口错误率。

如果第一批只服务少量机场，可先用单体应用和定时任务，暂不拆分微服务。

## 10. 合规与安全

- 仅接入允许公开展示和商业使用的数据。
- 军事、政府、医疗救援和明确要求隐藏的航空器不得实时公开追踪。
- 对敏感航空活动采用不展示、延迟展示或降低位置精度策略。
- 用户上传图片必须记录作者、授权范围和撤回机制。
- 页面始终提示航班可能临时调整，以机场及航司官方信息为准。
- API 密钥只存放在服务端环境变量中，不进入浏览器包或代码仓库。

## 11. 性能与可访问性目标

- 首屏核心内容在普通移动网络下尽量于 2.5 秒内可交互。
- 机场查询接口目标 P95 响应时间低于 800 毫秒。
- 所有按钮和输入项具备可识别的文本或 `aria-label`。
- 键盘可操作主要流程，按 `Esc` 可关闭详情侧栏。
- 颜色不是表达航班状态的唯一手段，同时保留文字标签。

## 12. 测试策略

当前骨架以 TypeScript 严格检查和生产构建作为基础门禁。继续开发时建议加入：

- Vitest：评分规则、机场查询和筛选逻辑单元测试。
- React Testing Library：搜索、错误状态、筛选和详情侧栏组件测试。
- Playwright：从查询机场到打开飞机详情的核心端到端流程。
- API 契约测试：确保真实后端响应符合 `AirportSnapshot` 领域模型。

## 13. 下一阶段实施顺序

1. 申请飞常准 Aviation MCP Key，以 AeroDataBox 为后备，使用 PVG、PEK、HKG 做字段、额度和覆盖率验收。
2. 建立 PostgreSQL 数据库，在供应商许可范围内积累到访频率和评分基线。
3. 增加彩绘机资料维护后台、来源引用和有效期审核。
4. 增加账户、机场收藏、注册号关注及通知订阅。
5. 补充自动化测试、监控、错误上报和部署流水线。

## 14. 当前交付验收标准

- 项目可以通过 `pnpm dev` 启动并通过 `pnpm build` 生产构建。
- 默认查询 PVG；没有密钥时明确提示，不显示虚构航班。
- 可按 47 个常用机场的代码、名称和城市查询，也接受任意 IATA/ICAO 代码。
- 飞常准旧版 V3 可作为机场计划第一顺位；新版 MCP Key 作为特别候选的第一校验顺位。
- 只配置 AeroDataBox 免费密钥仍可生成机场当日快照；ADSB.lol 无需密钥并可单独关闭。
- 12 小时快照、24 小时失败回退、1 req/s 限速及 500 units 月度安全预算有效。
- 每架飞机显示数据来源和可信度，供应商状态在机场概览下方可见。
- 分类筛选正确，空分类有明确提示。
- 点击飞机卡片可以查看详情，点击遮罩或按 `Esc` 可以关闭。
- 桌面端与移动端均保持清晰可用。
